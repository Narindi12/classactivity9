name: classactivity9
description: A Flutter app demonstrating SQLite integration using sqflite.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.2.2
  path_provider: ^2.0.11
  path: ^1.8.2
  provider: ^6.0.5 # For state management
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
